# Logs

Native cTrader build, metadata, preflight and validation logs are stored here. Logs must never contain passwords, OTP/2FA values, API keys, access tokens or broker credentials. Account identifiers must not be copied into published reports.
