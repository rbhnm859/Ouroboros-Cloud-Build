name: Fibonacci XAUUSD v4 - Commercial Risk Compound Validation

on:
  workflow_dispatch:
  push:
    branches:
      - fibonacci-xauusd-v4-commercial
    paths:
      - '.github/workflows/backtest-fibonacci-xauusd-v40-risk-compound.yml'
      - 'FibonacciHarmonicSniperUltimate/versions/xauusd_3/tools/v40_risk_compound_validate.sh'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - uses: actions/checkout@v4
      - name: Run commercial risk compound validation
        shell: bash
        env:
          CTRADER_CTID: ${{ secrets.CTRADER_CTID }}
          CTRADER_ACCOUNT: ${{ secrets.CTRADER_ACCOUNT }}
          CTRADER_PASSWORD: ${{ secrets.CTRADER_PASSWORD }}
        run: bash FibonacciHarmonicSniperUltimate/versions/xauusd_3/tools/v40_risk_compound_validate.sh
      - name: Upload risk compound package
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xauusd-v40-risk-compound-validation
          path: FibonacciHarmonicSniperUltimate/versions/xauusd_3/validation-v40-risk/
          retention-days: 60
