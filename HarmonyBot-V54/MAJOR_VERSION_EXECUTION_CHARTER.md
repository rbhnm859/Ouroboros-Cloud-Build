# HarmonyBot V54 — Universal Harmonic Liberation & Fibonacci Grid Alpha Core

V54 is a clean major-version experiment from tested V53 HEAD. All 14 project roles remain active with dynamic weights. Risk, Model Validation, Red Team and Data Governance retain fail-closed veto.

## Frozen evidence
- Preserve V46/V51 high-quality Alpha semantics; expansion must be additive, never destructively replace proven core.
- Preserve V52 detector identity reconstruction and bounded pivot graph.
- Preserve V53 immutable A/B/C snapshot governance and Conversion Truth Ledger.
- V53 broad conversion is not promoted: its marginal cohort was negative.
- Fibonacci Grid is promoted to a causal research core because both V52-control and V53 conversion showed Grid > L0-only in all A/B/C windows, but final Grid V3 still requires isolated validation.

## V54 modules
1. Universal Harmonic Liberation: a family identity that fails a generic quality floor may continue as a traceable liberation hypothesis instead of silent death. It still must pass family confirmation, RR>=2, structural stop, risk, broker and execution legality.
2. Core Alpha Preservation: proven core candidates receive portfolio arbitration protection; expansion cannot silently starve them.
3. Family Grid Alpha Core V3: family-native Fibonacci fractions/weights only; no loss-dependent sizing, no recovery, no Martingale/DCA.
4. State-Aware Grid V3: deeper pending legs cancel when family-specific MFE proves the thesis is already moving.
5. Grid Leg Attribution: final analyzer must compare L0-only, legacy grid and Grid V3 economics.
6. Orthogonal context remains evidence/quality context, not a conventional-indicator hard gate.

## Fixed DEV variants
- V51_QUALITY_CORE_REPLAY
- V52_LIBERATION_CONTROL
- V54_LIBERATION_LEGACY_GRID
- V54_LIBERATION_GRID_V3
Each on immutable DEV-A/B/C. No post-result threshold tuning.

## Gates
Commercial minimum: >=90 baskets/1.5y; >=60/year; Net>=1800; PF>=2.0; expectancy>=20; WR>=50%; DD<=6%; A/B/C positive; unique=baskets; engineering/risk clean.
Core preservation: core cohort Net/PF may not materially degrade versus the fixed core replay and may not be lost through expansion slot starvation.
Expansion admission: marginal expansion Net>0, expectancy>0, PF>=1.25, A/B/C marginal Net>=0.
Universal liberation: every detected family must have traceable downstream disposition; zero execution is legal only when attributable to family/economic/risk/broker gates, never silent generic starvation.
Grid V3 promotion: must beat the same V54 liberation kernel with legacy grid on Net and expectancy without worsening PF or DD gate, and positive delta Net in >=2/3 windows.
Fresh/OOS remains untouched until DEV + capital + freeze hashes pass.
